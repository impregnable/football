- Change frontend to work properly with backend
- Add moment.js to frontend (bower install with --save)
- Instead of raw date, display formatted date in players list. Use Angular filters for conversion.
- Separate controllers, services and apps
